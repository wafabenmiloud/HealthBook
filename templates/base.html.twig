<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}HealthCare - Your Health Companion{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>

    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #ffffff;
        }
        .navbar {
            background-color: #9aa2dc; /* Green medical theme */
            padding: 10px 4%
        }
        .navbar-brand {
            font-weight: bold;
            font-size: 1.5rem;
            color: #ffffff !important;
        }
        .navbar-nav .nav-link {
            color: #ffffff !important;
        }
        .navbar-nav .nav-link:hover {
            border: 1px solid #ffffff;
            border-radius: 5px;
        }
        .btn-primary {
            background-color: #ffffff;
            color: #9aa2dc;
            border: 2px solid #9aa2dc;
        }
        .btn-primary:hover {
            background-color: #9aa2dc;
            color: #ffffff;
        }
        footer {
            background-color: #9aa2dc;
        }
        footer a {
            color: #ffffff;
            text-decoration: underline;
        }
        .hero {
            background:
                        url('{{ asset("images/bg.png") }}');
            background-size: cover;
            background-position: center;
            color: black;
            text-align: center;
            height:100vh;
            display:flex;
            flex-direction:column;
            justify-content: center
        }
        .hero h1 {
            font-size: 3rem;
            font-weight: bold;
        }
        .hero p {
            font-size: 1.2rem;
            margin-top: 1rem;
        }
        .container {
            margin-top: 3rem;
        }
    </style>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <a class="navbar-brand" href="{{ path('home') }}">HealthCare</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item"><a class="nav-link" href="{{ path('home') }}">Home</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('doctor_list') }}">Doctors</a></li>

 {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                       <li class="nav-item"><a class="nav-link" href="{{ path('appointment_list') }}">Appointments</a></li>

    {% endif %}

               
            </ul>
             {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <a class="btn btn-primary ml-3" href="{{ path('app_logout') }}">Logout</a>
    {% else %}
        <a class="btn btn-primary ml-3" href="{{ path('app_login') }}">Login</a>
    {% endif %}
        </div>
    </nav>

   

    <!-- Main Content -->
    <div class="container">
        {% block body %}{% endblock %}
    </div>

    <!-- Footer -->
    <footer class="text-white mt-5">
    <div class="container py-4">
        <div class="row">
            <div class="col-md-4">
                <h5>About HealthCare</h5>
                <p>
                    HealthCare is your trusted health companion, connecting you with the best healthcare services and professionals. Your health, our priority.
                </p>
            </div>
            <div class="col-md-4 text-center">
                <h5>Quick Links</h5>
                <ul class="list-unstyled">
                    <li><a href="{{ path('home') }}" class="text-white">Home</a></li>
                    <li><a href="{{ path('doctor_list') }}" class="text-white">Doctors</a></li>
                     {% if is_granted('IS_AUTHENTICATED_FULLY') %}

                    <li><a href="{{ path('appointment_list') }}" class="text-white">Appointments</a></li>
                        {% endif %}

                </ul>
            </div>
            <div class="col-md-4 text-center">
                <h5>Follow Us</h5>
                <div>
                    <a href="#" class="text-white mx-2"><i class="fab fa-facebook fa-lg"></i></a>
                    <a href="#" class="text-white mx-2"><i class="fab fa-twitter fa-lg"></i></a>
                    <a href="#" class="text-white mx-2"><i class="fab fa-instagram fa-lg"></i></a>
                    <a href="#" class="text-white mx-2"><i class="fab fa-linkedin fa-lg"></i></a>
                </div>
            </div>
        </div>
        <hr class="bg-white">
        <div class="text-center">
            <p>&copy; 2024 HealthCare. All Rights Reserved.</p>
            <p>
                <a href="#" class="text-white">Privacy Policy</a> |
                <a href="#" class="text-white">Terms of Service</a>
            </p>
        </div>
    </div>
</footer>

</body>
</html>
